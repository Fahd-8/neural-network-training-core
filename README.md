# neural-network-training-core
A ground-up study of PyTorch neural network training — starting from Python OOP and building toward a complete training loop.  Covers the math and intuition behind every component: nn.Module inheritance, forward pass, loss functions, backpropagation, and optimizers (SGD, Momentum, Nesterov, Adam, AdamW).
